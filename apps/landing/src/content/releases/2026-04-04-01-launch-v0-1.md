---
date: 2026-04-04
order: 1
tag: Launch
title: "Completr v0.1 — the project begins"
icon: foundation
tint: brand
landing:
    featured: true
    pitch: "El primer commit público de Completr: la arquitectura base sobre la que se construye todo lo demás, desde el catálogo hasta el sistema social."
highlights:
    - icon: code
      tint: brand
      text: "Project skeleton up and running: TypeScript, Express 5, Sequelize 6, PostgreSQL and Docker Compose. Modular `src/` layout ready to scale."
    - icon: shield
      tint: purple
      text: "Auth from day one: JWT plus bcrypt, four roles (user, premium, moderator, admin) and differentiated rate limiting per role."
    - icon: bug_report
      tint: warning
      text: "Three-layer error system (ServiceError → DomainError → HttpError) with global normalizers and per-module mappers — consistent diagnostics across the whole API."
    - icon: search
      tint: success
      text: "Game catalog backed by RAWG: `searchGame`, `getGameById`, `getGameBySlug` with filters and typed errors for not-found, rate-limited and request errors."
    - icon: storage
      tint: brand
      text: "Three-tier scoring model: `GameScore`/`GameTime` global per source, `GameShelf` per user and `ListItem` with values frozen into lists."
---
