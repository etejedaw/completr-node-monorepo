info:
    name: Get My Wishlist
    type: http
    seq: 3

http:
    method: GET
    url: "{{BASE_URL}}/users/me/wishlist"
    auth: inherit

settings:
    encodeUrl: true

docs: |-
    ## Get My Wishlist

    Returns the authenticated user's wishlist ordered by position.

    **Authentication:** Bearer token required.
    **Rate Limit:** 50 requests per minute.

    ### Response (200)

    ```json
    {
      "data": {
        "wishlist": [
          {
            "id": "uuid",
            "position": 1,
            "backlog": {
              "id": "uuid",
              "status": "not_started",
              "score": 89,
              "duration": 6,
              "startedAt": null,
              "notes": null,
              "game": { "id": "uuid", "title": "...", "coverUrl": "...", "isDlc": false },
              "platform": { "id": "uuid", "abbreviation": "..." }
            }
          }
        ]
      }
    }
    ```
